apiVersion: certmanager.k8s.io/v1alpha1
kind: Certificate
metadata:
  name: {{.SGMR_SERVICE_NAME}}-pod-tls
spec:
  secretName: {{.SGMR_SERVICE_NAME}}-pod-tls
  issuerRef:
    name: platform-tls
    kind: ClusterIssuer
  commonName: {{.SGMR_SERVICE_NAME}}.{{.KUBE_NAMESPACE}}.svc.cluster.local
  dnsNames:
  - localhost
  - 127.0.0.1
