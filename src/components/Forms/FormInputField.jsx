import React from 'react';

const FormInputField = () => {
  return (
   <div>
     Not currently using this, specifying each input field individually and will revisit refactoring them out to components later
   </div>
  );
};

export default FormInputField;
